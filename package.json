{
	"name": "healthcheck-api",
	"description": "A lightweight and extensible Health Check API to monitor and report the status of system components and services. Ideal for microservices, containers, and distributed systems.",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"hono": "^4.7.11"
	},
	"devDependencies": {
		"wrangler": "^4.4.0"
	}
}